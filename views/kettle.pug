extends layout

block content
  h1= title
  - let items=results
  //- div(class="container-fluid")
  //- div(class="row")
  //- each value,index in items
  //-   div(class="col-sm-2" onclick=`buy(${index})`)  #{value.kettle_name} kettle, quantity is #{value.quantity}, with #{value.resistance}
  //- script. 
  //-   function buy(index)
  //-   {
  //-     alert(`You purchased Item ${index+1}`)
  //-   } 
  div(class="container-fluid")
    div(class="row m-1")
     each item in items
      - let destination = `dispatch(\"/kettles/update?id=${item._id}\")`
      div(class="row-sm-2", class="m-1") kettle #{item.kettle_name} quantity (#{item.quantity}) is #{item.resistance}
       button.btn( class="btn-primary ml-1", onclick= destination) Update


  script. 
   function dispatch(path){
    window.location.href=path
    }