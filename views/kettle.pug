extends layout

block content
  h1= title
  - let items=results
  div(class="container-fluid")
  div(class="row")
  each value,index in items 
    div(class="col-sm-2" onclick=`buy(${index})`) #{value.kettle_name} , quantity is #{value.quantity} , with #{value.resistance} resistance
  script. 
    function buy(index)
    {
      alert(`You purchased Item ${index+1}`)
    } 